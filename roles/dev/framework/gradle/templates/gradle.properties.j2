{{ note.cfg }}
{% if is_proxy %}
systemProp.http.proxyHost={{ proxy.through.host }}
systemProp.http.proxyPort={{ proxy.through.port }}
systemProp.https.proxyHost={{ proxy.through.host }}
systemProp.https.proxyPort={{ proxy.through.port }}
{% endif %}

