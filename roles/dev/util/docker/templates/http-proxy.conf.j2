{{ note.cfg }}
[Service]
{% if is_proxy %}
Environment="HTTP_PROXY=http://{{ proxy_through.host }}:{{ proxy_through.port }}"
Environment="HTTPS_PROXY=http://{{ proxy_through.host }}:{{ proxy_through.port }}"
{% endif %}

