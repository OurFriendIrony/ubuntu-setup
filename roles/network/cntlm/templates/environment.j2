{{ note.cfg }}
PATH="/usr/local/sbin:/usr/local/bin:/usr/sbin:/usr/bin:/sbin:/bin:/usr/games:/usr/local/games"
{% if is_proxy %}
https_proxy=http://{{ proxy.through.host }}:{{ proxy.through.port }}
http_proxy=http://{{ proxy.through.host }}:{{ proxy.through.port }}
{% endif %}

