---

- hosts         : windows
  connection    : local
  gather_facts  : yes

  vars:
    - ansible_user                          : "steve.stevens@live.co.uk"
    - ansible_password                      : "-CHANGEME-"
    - ansible_connection                    : "winrm"
    - ansible_winrm_transport               : "ntlm"
    - ansible_winrm_server_cert_validation  : "ignore"
  roles:
    - { role: checks                                                                                            }
    - { role: windows                                                                                           }
    - { role: wallpaper                                                                                         }
    - { role: browser/chrome                                                                                    }
    - { role: bash                                                                                              }
    - { role: vim                                                                                               }
  tasks:
    - name: "Ping Test"
      win_ping:

